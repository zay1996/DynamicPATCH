[tool.poetry]
name = "DynamicPATCH"
version = "0.2.0"
description = "Python package DynamicPATCH"
authors = ["Aiyin Zhang"]
license = "MIT"
readme = "README.md"

include = [
    { path = "dynamicpatch/static" }
]

[tool.poetry.dependencies]
python = "^3.10"
numpy = ">=1.21.3,<2.0.0"
pandas = "^2.2.2"
matplotlib-scalebar = "^0.8.1"
matplotlib = "^3.9.2"
opencv-python = "^4.5.3"
scikit-image = "^0.22.0"
openpyxl = "^3.1.5"
xarray = "^2025.4.0"
rioxarray = "^0.19.0"
dask = "^2025.4.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.2"
black = "^24.8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
